---
import type { Project } from "../lib/project"
//import Link from "./Link.vue"

type Props = {
    project: Project
}

const { project: { repo: name, description, link } } = Astro.props
---

<div class="flex flex-col">
    <!--<Link href={link}>-->
    <!--    <span class="font-medium">{name}</span>-->
    <!--</Link>-->
    <p><a class="underline decoration-neutral-600 underline-offset-4 transition-colors focus:(decoration-neutral-500 outline-offset-6) hover:decoration-neutral-500"
          href={link}>{name}</a></p>

    <span class="pt-3">{description}</span>
</div>